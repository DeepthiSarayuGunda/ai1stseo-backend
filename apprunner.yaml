version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip3 install -r requirements.txt

run:
  command: gunicorn application:application --bind 0.0.0.0:8080 --workers 4 --timeout 120
  network:
    port: 8080
